# Validate Prompt Test

This is a sample prompt for testing the validate tool.

## Task
Validate the following configuration:

```json
{
  "name": "test-project",
  "version": "1.0.0",
  "dependencies": {
    "express": "^4.18.0"
  }
}
```

## Expected Output
- Validate configuration structure
- Check for missing required fields
- Verify dependency versions
# Migration Prompt Test

This is a sample prompt for testing the migration tool.

## Task
Analyze the following code and suggest migration strategies:

```javascript
// Legacy code example
function oldFunction() {
  var x = 10;
  var y = 20;
  return x + y;
}
```

## Expected Output
- Identify legacy patterns
- Suggest modern alternatives
- Provide migration steps